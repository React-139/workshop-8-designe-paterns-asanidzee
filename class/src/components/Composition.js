import React from "react";

const Composition = ({ children }) => {
    return (
        <div>
            <h1>Compositon</h1>
            {children}
        </div>
    );
};

export default Composition;
